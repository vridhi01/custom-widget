/**
 *  This is used for slider
 */
var config = {
    paths: {
        'owlcarousel': "js/owl.carousel.min"
    },
    shim: {
        'owlcarousel': {
            deps: ['jquery']
        }
    }
}